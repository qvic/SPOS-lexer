package token;

public enum TokenType {
    KEYWORD,
    IDENTIFIER,
    DELIMITER,
    OPERATOR,
    INTEGER,
    FLOAT,
    LONG_INTEGER,
    IMAGINARY_NUMBER,
    STRING,
    INDENT,
    DEDENT,
    ERROR_SEQUENCE,
    NEWLINE
}